- name: Installing Stow
  become: true
  pacman:
    name: [
      "stow"
    ]
  tags:
    - dotfiles
    - dev

- name: Cloning dev dotfiles
  ansible.builtin.git:
    repo: git@github.com:pinnheads/.dev.git
    dest: "{{ lookup('env', 'HOME') }}/.dev"
    recursive: yes
    update: yes
    accept_hostkey: yes
    version: main
  tags:
    - dotfiles
    - dev

- name: Stow dotfiles
  shell: cd $HOME/.dev && ./stow
  tags:
    - dotfiles
    - dev
